# RUN — 좌측 바디에서 컨트롤러 시트·USB 홀 제거
slug: left-body-usb-seat-removal

## 실행 방식
Dynamic Workflow 없이 이 세션에서 직접 실행. 사유: 변경이 호출 인자 1곳(좌측
`build_body`)뿐인 소규모 작업이고, 슬라이스 S1→S2가 순차 의존이라 병렬화 이득이 없음
(fg-run "Estimate cost first" — 전례: rp2040 태스크와 동일 판단).

## Work slices 결과

### S1. 좌측 build_body 호출 변경 + 재실행 — **완료**
- `create_keyboard_plates.py` 좌측 호출을 `include_controller` 기본값(False)으로 축소,
  `jack_center_x`만 키워드 인자로 유지. MASTER_RIGHT·BOOTSEL 근거를 주석으로 남김.
- FreeCAD MCP로 스크립트 무오류 재실행, FCStd 저장, `left_keyboard_body.stl` 재생성 확인
  (mtime 갱신).
- FCStd에 `Left_RP2040_Zero_Reference` 부재, `Right_RP2040_Zero_Reference` 존재 확인.

### S2. 기하 검증 — **완료 (전 항목 통과)**
- 구 USB 관통홀 영역(10×3×6 프로브): 공통 부피 **180.0/180.0** — 리어월 완전 솔리드.
- 구 시트 리세스 영역(19.2×27.2×1, z −16~−15): **522.24/522.24** — 바닥 솔리드.
- PJ-322 잭 홀 내부(4×3×4 프로브): 공통 부피 **0.000** — 홀 유지.
- 잭 뒷막이(12×3×2 프로브): **72.0/72.0** — 유지.
- 우측 바디 부피: 변경 전 210,748.481 → 후 210,748.481 (delta −0.0005 ≈ 부동소수 오차)
  — **무변화 확인**.
- 좌측 바디 부피: 160,744.1 → 161,481.8 (+737.7 mm³) — 제거된 컷(홀·베젤·시트)이
  채워진 만큼 증가, 예상과 부합.

## 계획과의 발산(divergence) 요약
1. **[낮음]** 발산 없음 — 두 슬라이스 모두 계획된 완료 기준 그대로 충족. 즉석 결정이나
   막힌 지점 없음.
2. **[참고]** 이 실행 직전, 활성 슬롯을 점유하던 rp2040-zero-split-firmware(검증 완료·
   회고 대기)를 사용자 승인 하에 회고 생략 봉인(`done/2026-07-12-…`)하고 슬롯을 비운 뒤
   본 플랜을 승격함 — 본 작업 자체의 발산은 아님.

## 다음 회고에서 다룰 만한 점
- `build_body`의 `include_controller` 플래그가 시트·홀·베젤·참조객체를 하나로 묶어 둔
  설계 덕에 좌우 비대칭화가 인자 1곳 변경으로 끝남 — 파라메트릭 스크립트에서 기능
  단위 플래그의 가치가 재확인됨.
